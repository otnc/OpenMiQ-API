// OpenMiQ-API itself has no version scheme (unlike @makeitaquote/miqx's
// /v1/make) — these are just the endpoints that exist.
export const QUOTE_PATH = "/api/quote";
export const FAKEQUOTE_PATH = "/api/fakequote";
export const USAGE_PATH = "/api/usage";
export const UPLOAD_PATH = "/api/uploads";
