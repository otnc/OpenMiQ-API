/**
 * The default target for this package: the copyright holder's own hosted
 * instance. Point `baseUrl` at your own self-hosted OpenMiQ-API instead if
 * you're running one.
 */
export const DEFAULT_BASE_URL = "https://miq.otnc.dev";

export const QUOTE_PATH = "/api/quote";
export const FAKEQUOTE_PATH = "/api/fakequote";
export const USAGE_PATH = "/api/usage";
